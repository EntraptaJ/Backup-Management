query Client($clientId: ID!) {
  client(clientId: $clientId) {
    path
    id
    schedules {
      id
      createdAt
      updatedAt
      time
    }
    backups {
      id
      updatedAt
      createdAt
      id
      state
      fileSize
    }
  }
}
