mutation CreateClient($serviceId: ID!, $input: CreateClientInput!) {
  createClient(serviceId: $serviceId, input: $input) {
    id
    name
    clients {
      id
      path
      folderSize
    }
  }
}
