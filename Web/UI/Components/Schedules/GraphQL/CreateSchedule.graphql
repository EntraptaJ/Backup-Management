mutation CreateSchedule($clientId: ID!, $input: CreateScheduleInput!) {
  createSchedule(clientId: $clientId, input: $input) {
    id
    path
    schedules {
      id
      createdAt
      updatedAt
      time
    }
  }
}
